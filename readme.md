Just a simple script to update and launch both BetterDiscord and Spicetify

# Installing

git clone https://github.com/Cloudiz1/bootup-script.git
cd bootup-script
pyinstaller src/main.exe
cd ../dist
make a shortcut of main.py
in run (win + r) type shell:startup
drag shortcut into directory
